{
  "compilerOptions": {
    "target": "es2016",                              /* Target modern JavaScript (ES2016) for compatibility with Node.js. */
    "module": "commonjs",                            /* Use CommonJS for module resolution (standard in Node.js). */
    "rootDir": "./src",                              /* Set the root directory for source files. */
    "outDir": "./dist",                              /* Specify the output directory for compiled files. */
    "strict": true,                                  /* Enable all strict type-checking options. */
    "esModuleInterop": true,                         /* Allow default imports from CommonJS modules. */
    "skipLibCheck": true,                            /* Skip type-checking of library declaration files for faster builds. */
    "forceConsistentCasingInFileNames": true,        /* Ensure consistent file name casing across the project. */
    "typeRoots": ["./src/types", "./node_modules/@types"] /* Specify where to look for type definitions. */
  },
  "include": ["src/**/*"],                           /* Include all files in the `src` directory. */
  "exclude": ["node_modules", "dist"]                /* Exclude `node_modules` and the build output directory. */
}
